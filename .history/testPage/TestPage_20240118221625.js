import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import CameraScreen from './CameraScreen'; // 更新为实际路径

export default function TestPage() {
  return (
    <View>
      <Text>TestPage</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
